[project]
name = "habitattrack-api"
version = "0.1.0"
description = "Habitat Track API"
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.104.0",
    "pydantic>=2.0.0",
    "google-cloud-firestore>=2.14.0",
    "python-dotenv>=1.0.0",
]
